# 测试环境配置 - 覆盖默认配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seckill_mall_test?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
  redis:
    database: 1
